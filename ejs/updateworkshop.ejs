<!DOCTYPE html>
<html lang="en">

<head>
    <% include ./head %>
</head>

<body class="container">
    <div class="workshop-update">
        <div class="media-body" style="border-bottom: solid 5px grey">
            <h5 class="mt-0 mb-1">
                <%= data.name %></h5>
            <%= data.description %>
        </div>
    </div>
    <form action="/update-workshop/<%= data.name %>" method="post">
        <div class="form-group">
            <label for="name">New name</label>
            <input type="text" name="name" class="form-control" id="name" placeholder="Workshop Name" required>
        </div>
        <div class="form-group">
            <label for="description">New description</label>
            <input type="text" name="description" class="form-control" id="description"
                placeholder="Give a description for your workshop" required>
        </div>
        <button type="submit" class="btn btn-primary">Save</button>
        <a type="button" class="btn btn-secondary" href="/">Cancel</a>
    </form>

    <footer>
        <% include ./footer %>
    </footer>

</body>

</html>